# Changelog

## v1.14.0

- 账号安全新增 TOTP 双因素认证：可在个人资料中扫码绑定、验证启用或使用当前密码和验证码停用。
- 启用双因素认证的账号登录时先完成短期验证码挑战，验证码支持标准身份验证器 App，并对登录与配置操作分别限流。
- 双因素认证密钥只在绑定时短暂返回，服务端使用已有设置加密密钥以 AES-256-GCM 密文保存；账号权限页展示保护状态。

## v1.13.1

- 修复 setup 镜像构建上下文遗漏数据库备份服务源码的问题，确保总控镜像可在 amd64/arm64 架构正常构建。

## v1.13.0

- 告警规则新增最高风扇转速（FAN_RPM）指标，覆盖触发、恢复和无风扇设备场景。
- 总控新增 PostgreSQL 数据库备份与恢复，支持恢复点列表、失败恢复、保留策略和按部署时区每日自动备份。
- 服务可用性监控新增 FTP 握手、SFTP/SSH 握手和 SNMP v2c 只读探测，SNMP community 以加密密文保存。

## v1.12.0

- 设备管理增加分组、环境和 Agent 健康状态筛选，并支持导出当前筛选后的设备清单（不包含 Agent 密钥）。
- 首页增加全局告警中心入口，服务监控增加数量、状态和平均可用率摘要，移动端布局同步优化。
- Agent 安装命令和部署文档统一使用总控同域入口，校验下载内容并限制连接超时；root 环境不再强制依赖 sudo，降低受限网络和最小化系统安装失败概率。

## v1.11.1

- 优化总控和 Agent 镜像拉取：默认优先使用可用性更高的 1ms 镜像源，单个镜像源默认 180 秒后快速切换，避免更新任务长时间卡住并被标记为异常。
- Windows 与 Linux 更新脚本、安装器和示例配置统一镜像源顺序，移除不稳定的公共镜像依赖。

## v1.11.0

- 设备管理支持资产编号、责任人、供应商、型号、序列号、环境、采购与保修日期及说明，并可在设备详情维护工作记录。
- 设备详情增加状态转换时间线，首页增加值班记录和多设备资源趋势，便于定位离线与交接问题。
- Agent 安装命令统一优先从总控同域下载，增加 wget 备用通道、有限重试和分层错误提示，降低 GitHub 出口受限导致的安装失败。

## v1.10.0

- 增加设备 Agent 接入健康诊断，在首页、设备列表和详情页说明待接入、离线及指标延迟原因。
- 总控同域安装器同步 Linux/Windows 安装脚本，Shell 文件强制使用 LF，避免 Windows 发布换行导致 Linux 安装失败。
- 安装器保留总控优先、IPv4 优先、有限重试和镜像回退，降低受限网络环境下的下载超时。

## v1.9.0

- 修复受监控服务器无法访问 GitHub Raw 时 Agent 无法安装的问题：安装命令优先从总控同域下载安装器，并保留 jsDelivr、GitHub Raw 回退。
- 告警列表支持设备、状态和严重级别筛选，以及批量确认待处理告警。
- 增强安装器下载超时、IPv4 优先和安装失败提示，降低受限网络环境下的长时间卡死风险。

## v1.8.0

- 修复 Agent 安装入口在 IPv6 路由不可用时访问 GitHub Raw 长时间超时的问题：安装命令现在优先通过 IPv4 访问 jsDelivr，并使用有限重试和超时，最后才回退到 GitHub Raw。
- 增加维护窗口、系统日志、自定义采集项和自定义指标告警支持，并在设备详情页展示相关数据。
- 增加网络拓扑视图、设备详情的运行时和日志信息，以及维护窗口管理页面。
- 增强 Agent 进程、连接、端口、容器和磁盘采集配置，并完善镜像拉取超时和 API Token 权限范围。

## v1.7.3

- 修复部分服务器 IPv6 路由不可用时 Agent 安装器下载和服务探测长时间超时的问题，改为 IPv4 优先并保留 IPv6 回退。

## v1.7.2

- Agent 安装命令兼容旧版 curl，移除 `--retry-all-errors`，避免 CentOS 8 等系统下载参数不兼容。

## v1.7.1

- Agent 安装命令优先使用 jsDelivr，失败时回退 GitHub Raw，并为下载增加重试与超时，适应受限网络环境。
- Agent 镜像拉取增加有限超时，避免网络异常导致安装任务长期卡住。

## v1.7.0

- Agent 支持按需采集完整进程清单，带 CPU、内存、状态、用户和命令行信息。
- 端口、容器和进程明细增加独立采集开关与安全上限，适用于高连接数、容器密集型和进程密集型主机。
- Linux/Windows Agent 安装器和设备管理页可生成完整进程采集参数。
- 设备详情页展示进程命令行，保留历史进程资源趋势。
